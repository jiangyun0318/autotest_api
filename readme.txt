7、支持jenkins集成，目前手动构建
6、环境分开处理（测试、预发、线上）

5、二期--优化
1）依赖数据类型判断，目前支持字符串与列表格式
2）优化下错误日志，出问题尽量好定位

4、二期--接入sql
1）接入sql，支持sql数据依赖，以及sql的预期结果比对
2）优化get请求处理，支持url与data字段的依赖处理
3）支持excel的多个sheet处理

3、二期--get请求处理
1）get请求数据放在data字段，支持数据依赖
2）get请求的url，支持参数赋值

2、支持多系统测试
1）目前支持三个系统：资运、crm、cms，根据用例设置，可同时生成三个系统的测试报告
2) 用例规则说明：case_XXX_X.excel
   'XXX'表示系统名称：ao(资运);crm;cms
   'X'：0表示该excel不执行、1表示执行
3）支持json的部分校验，规则暂不说明

1.自动化框架第一期-实现功能点
1）用例excel文件的读、写相关操作，方法封装
2）token处理，若有登录接口，获取返回的token，默认为本人账号；
3）支持指定接口跳过测试；
4）配置文件读取功能（目前：server.ini、header.json）
5）支持单个接口测试，包括post、get、put与peach；
6）预期结果处理：json格式、msg、code；
7）单个依赖数据处理（获取某个接口返回数据，赋值当前接口具体字段）
8）测试报告（HTML）以及邮件发送
